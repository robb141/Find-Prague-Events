[tool.ruff]
line-length = 100
target-version = "py313"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B", "SIM"]
ignore = [
    "E501",  # long regex/URL literals are common in the scraper
]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["B018"]
